describe('delivery cost tester function', () => {
  beforeAll(async () => {});

  // Assert if setTimeout was called properly
  it('calculate discount: scenario 1', () => {});

  // Assert greeter result
  it('greets a user with `Hello, {name}` message', () => {});
});
